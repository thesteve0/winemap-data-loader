apiVersion: batch/v1
kind: Job
metadata:
  name: job
spec:
  template:
    metadata:
      name: job
    spec:
      containers:
      - name: job
        image: 172.30.1.1:5000/myproject/loader
        command: ["python", "app.py"]
      restartPolicy: Never
